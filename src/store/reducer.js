const initialState = {
  totalPrice: 0,
  dishesInCart: [],
};
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};
export default reducer;
